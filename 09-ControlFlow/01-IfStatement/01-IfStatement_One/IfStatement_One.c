#include<stdio.h>
int main(void)
{
	//variable declarations
	int a_olp, b_olp, p_olp;

	//code
	a_olp = 9;
	b_olp = 30;
	p_olp = 30;

	printf("\n\n");
	if (a_olp < b_olp) {
		printf("A is Less than B !!!\n\n");
	}
	if (b_olp != p_olp) {
		printf("B is Not Equal To P !!!\n\n");
	}
	printf("Bot Comparison Have Beed Done!!!\n\n");

	return (0);
}